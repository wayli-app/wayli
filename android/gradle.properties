org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
# Configuration cache disabled until GitHub Packages auth is wired (composite build workaround)
# org.gradle.configuration-cache=true
android.useAndroidX=true
android.nonTransitiveRClass=true
# compileSdk 37 (Android 17) is ahead of AGP 8.10's tested maximum (36);
# full support lands in AGP 9.1.1. The build works fine — silence the
# recommendation until the next AGP upgrade.
android.suppressUnsupportedCompileSdk=37.0
kotlin.code.style=official
